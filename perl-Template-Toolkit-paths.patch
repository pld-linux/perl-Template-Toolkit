diff -durN Template-Toolkit-2.07.orig/Makefile.PL Template-Toolkit-2.07/Makefile.PL
--- Template-Toolkit-2.07.orig/Makefile.PL	Thu Jan 17 10:33:14 2002
+++ Template-Toolkit-2.07/Makefile.PL	Thu Jul 11 08:39:42 2002
@@ -189,20 +189,20 @@
     return '' unless $main::TT_PREFIX;
     my $amble =<<'EOF';
 tt2_install ::
-	@$(PERL) bin/tt2inst -v "$(TT_PREFIX)"
+	@$(PERL) -Mblib bin/tt2inst -v "$(DESTDIR)$(TT_PREFIX)"
 
 tt2_splash ::
-	@$(PERL) -I$(INST_LIB) -I$(INST_ARCHLIB) bin/gifsplash -v -i "$(TT_PREFIX)"
+	@$(PERL) -Mblib -I$(INST_LIB) -I$(INST_ARCHLIB) bin/gifsplash -v -i "$(DESTDIR)$(TT_PREFIX)"
 
 EOF
     $amble .=<<EOF if $main::TT_BUILD_DOCS;
 tt2_html_docs ::
-	\@\$(PERL) -I\$(INST_LIB) -I\$(INST_ARCHLIB) bin/ttree -v -f "\$(TT_PREFIX)/$TT_DOCS_CFG"
+	\@\$(PERL) -Mblib -I\$(INST_LIB) -I\$(INST_ARCHLIB) bin/ttree -v -f "\$(DESTDIR)\$(TT_PREFIX)/$TT_DOCS_CFG"
 
 EOF
     $amble .=<<EOF if $main::TT_EXAMPLES;
 tt2_examples ::
-	\@\$(PERL) -I\$(INST_LIB) -I\$(INST_ARCHLIB) bin/ttree -v -f "\$(TT_PREFIX)/$TT_EXAMPLE_CFG"
+	\@\$(PERL) -Mblib -I\$(INST_LIB) -I\$(INST_ARCHLIB) bin/ttree -v -f "\$(DESTDIR)\$(TT_PREFIX)/$TT_EXAMPLE_CFG"
 
 EOF
     return $amble;
